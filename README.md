# Prompt Hub

简易的静态 Prompt / Files 展示页。

## 用法
1. 把 prompt 放到 `prompts/`，文件放到 `files/`。
2. 运行 `python3 scripts/build-manifest.py`。
3. 打开 `index.html` 或发布到 GitHub Pages。

## 一键启动
- 双击 `run.command`

## 网页
- https://byliangg.github.io/prompt/
